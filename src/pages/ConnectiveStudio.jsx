import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export const ConnectiveStudio = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>CONNECTIVE Studio</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is a placeholder for the CONNECTIVE Studio component.</p>
      </CardContent>
    </Card>
  );
};